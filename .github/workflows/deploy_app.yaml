name: deploy_app

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: churn-service
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch raw dataset
        run: |
          mkdir -p data/raw
          curl -sL -o data/raw/Telco-Customer-Churn.csv \
            https://raw.githubusercontent.com/IBM/telco-customer-churn-on-icp4d/master/data/Telco-Customer-Churn.csv

      - name: Train model
        run: |
          python -m training.src.process
          python -m training.src.train_model

      - name: Save model to BentoML store
        run: python application/src/save_model_to_bentoml.py

      - name: Build Bento
        run: bentoml build

      - name: Containerize Bento
        # --opt platform=... triggers NotImplementedError in bentoml 1.2;
        # default platform on Ubuntu runners is already linux/amd64.
        run: bentoml containerize churn_service:latest

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag and push image
        run: |
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          IMAGE_BASE="${{ env.REGISTRY }}/${OWNER_LC}/${{ env.IMAGE_NAME }}"
          BENTO_TAG=$(bentoml get churn_service:latest --output tag)
          docker tag "${BENTO_TAG}" "${IMAGE_BASE}:${{ github.sha }}"
          docker tag "${BENTO_TAG}" "${IMAGE_BASE}:latest"
          docker push "${IMAGE_BASE}:${{ github.sha }}"
          docker push "${IMAGE_BASE}:latest"
          echo "Pushed ${IMAGE_BASE}:{${{ github.sha }},latest}"
